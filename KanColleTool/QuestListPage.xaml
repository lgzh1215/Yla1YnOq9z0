<Page x:Class="KanColleTool.QuestListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      xmlns:local="clr-namespace:KanColleTool"
	Title="QuestListPage" Loaded="Page_Loaded">

    <DataGrid AlternationCount="2" AutoGenerateColumns="False" RowHeaderWidth="0" 
              SelectionMode="Extended" HorizontalAlignment="Stretch" Name="QuestGrid" 
              VerticalAlignment="Stretch" AlternatingRowBackground="LemonChiffon">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Path=api_no.Value}" />
            <DataGridTextColumn Header="完成度" Binding="{Binding Path=api_progress_flag.Value}" />
            <DataGridTextColumn Header="任務" Binding="{Binding Path=api_title.Value}" />
            <DataGridTextColumn Header="詳細" Binding="{Binding Path=api_detail.Value}" />
        </DataGrid.Columns>
    </DataGrid>
</Page>
